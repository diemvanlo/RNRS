let convert;
export default convert = (html: string) => {
    // html = html.replace("<p>","<View>");
    // html = html.replace("</p>","</View>");
    // html = html.replace("</span>","");
    console.log(html);
    return html;
}